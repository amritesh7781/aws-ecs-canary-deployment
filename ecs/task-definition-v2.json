{
  "family": "canary-demo-canary",
  "networkMode": "awsvpc",
  "requiresCompatibilities": ["FARGATE"],
  "cpu": "256",
  "memory": "512",
  "executionRoleArn": "arn:aws:iam::ACCOUNT_ID:role/ecsTaskExecutionRole",
  "containerDefinitions": [
    {
      "name": "canary-demo",
      "image": "ACCOUNT_ID.dkr.ecr.REGION.amazonaws.com/canary-demo:v2",
      "portMappings": [
        {
          "containerPort": 3000,
          "protocol": "tcp"
        }
      ],
      "environment": [
        { "name": "VERSION",       "value": "v2" },
        { "name": "VERSION_LABEL", "value": "2.0.0" },
        { "name": "BUILD_TIME",    "value": "2026-04-02T00:00:00Z" }
      ],
      "healthCheck": {
        "command": ["CMD-SHELL", "wget -qO- http://localhost:3000/health || exit 1"],
        "interval": 15,
        "timeout": 5,
        "retries": 3,
        "startPeriod": 10
      },
      "logConfiguration": {
        "logDriver": "awslogs",
        "options": {
          "awslogs-group": "/ecs/canary-demo",
          "awslogs-region": "us-east-1",
          "awslogs-stream-prefix": "canary"
        }
      },
      "essential": true
    }
  ]
}
